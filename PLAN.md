# azure-databricks — PLAN

Roadmap for the medallion line. `0.0.1` (Kimball) is frozen; all new work lands in the `0.1.x` series.

| Milestone | Scope                                                                                                                                                                                                                                                                                                                                                                                             |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `0.1.0`   | Initial medallion: 6 source schemas, bronze (precedence + provenance), silver `investments` (23 entities, 8 SCD2 dims), gold (5 PD-strategy teams + `gold_pd_consolidated`). 5 MV-placement scenarios × 3 demo queries. **Status:** validated through silver materialization on Free Edition; gold MV materialization hit 100/schema cap + 2.5h+ cascade. Retained as legacy reference.           |
| `0.1.1`   | **Schema split + cascading MVs + base framework.** Silver splits to `investments` (18 current: 8 SCD2 dims + fx_rate_dim + 9 facts incl. new `vtransaction_fact`) + `investments_history` (6 monthend/cancels/bridge) — under Free Edition 100/schema cap. Mv* bodies reference upstream mv* (silver→bronze, gold→silver) — gold materialization drops 2.5h+ → ~5–10 min. Byte-equality contract relaxed to mechanically-derivable. Adds `04_gold/05_refresh_procs.sql` (60 procs), `gold_pd_consolidated` views/MVs (cross-team UNION), `05_validate/` correctness gates (6 files), `06_demos/` v-vs-mv-vs-t pedagogy harness (10 files), plus a thin Asset Bundle + bash runbook (`databricks.yml`, `scripts/runbook.sh`) — foundation for the 0.1.7 CI runner. |
| `0.1.2`   | **Non-PD gold schemas — landed 2026-05-13.** Adds `team_re_core`, `team_re_value_add`, `team_pe_buyout`, `team_infra`, `team_public_equity` — 5 schemas × 10 entities × (t + v + procs) = 50 tables + 50 views + 55 procs. Mirrors team_pd_distressed shape; MVs deliberately skipped per option-B pattern (each adds ~5 min cold-start on Free Edition). Strengthens cross-team-MV-reuse demo (S2). Orchestrator extended with 5 new `refresh_all()` calls.                                                                                                                                                                                                                                       |
| `0.1.3`   | `_all` historical UNION pattern (3 silver entities gain `*_all` siblings unioning current + historical, leveraging the `investments_history` schema introduced in 0.1.1) **and** `_pivot` cross-tab pattern (2–3 silver entities gain `*_pivot_*` siblings using the PIVOT operator at view time). Both are deferred enterprise patterns from DECISIONS.md #7. |
| `0.1.4`   | MV `SCHEDULE` clauses for production refresh. Removes manual-trigger demo posture.                                                                                                                                                                                                                                                                                                                |
| `0.1.5`   | Bloomberg security pricing layer. Bronze precedence rule update for `vsecurity_price` (state_street vs bloomberg).                                                                                                                                                                                                                                                                                |
| `0.1.6`   | PBIP fixture coupled-landing — repoint `fixtures/azure-databricks.pbip` from 0.0.1's Kimball mart schemas to 0.1.1's `investments` + `team_pd_*` + `gold_pd_consolidated`. (PBIP fixture lives in a sister repo; this milestone tracks the schema-name realignment.)                                                                                                                              |
| `0.1.7`   | CI smoke runner — automated end-to-end run on a paid Databricks workspace via Databricks CLI / REST. Foundation laid in 0.1.1 base framework (`databricks.yml` Asset Bundle stub + `scripts/runbook.sh`); 0.1.7 adds the GitHub Actions wiring + paid-workspace credentials.                                                                                                                          |
| `0.1.8`   | Streaming-table option for bronze (real-time-ish ingestion) — alternative to batch refresh procs.                                                                                                                                                                                                                                                                                                 |
| `0.2.0`   | Real eFront schema integration if user supplies authoritative shapes. Removes industry-standard inferences flagged in DECISIONS.md.                                                                                                                                                                                                                                                               |

## Deferred / open questions

- **Multi-catalog deploy automation** — catalog name is a session var (`01_config.sql`) and a CLI env var (`DATABRICKS_CATALOG` in `.env`); 0.1.1 lays bash + bundle foundation but isolated per-branch/PR catalogs still require manual setup. A Terraform module or `databricks bundle` target-per-PR pattern would automate this.
- **Refresh proc vs MV split** — DECISIONS.md #5 documents the operational distinction; 0.1.1's `06_demos/02_what_is_an_mv.sql` and `03_what_is_a_table.sql` make the distinction explicit pedagogically. The S0–S4 placement scenarios still group them as "materialized"; a 0.1.4 follow-on could add a dedicated "table-refresh" scenario.
- **Free Edition compute budget** — bronze MV initial materialization is the heavy phase (~5–10 min). Smaller `seed_n_*` defaults already chosen but worth revisiting if Free Edition users hit quota.
- **Silver vtransaction_fact downstream consumers** — 0.1.1 added `investments.vtransaction_fact` to back `gold_pd_consolidated.vpd_transaction_book` (DECISIONS.md #15). It's currently consumed only at the consolidated layer. Per-team transaction views (`team_pd_*.vtransaction_fact`) would let team gold also consume transactions; deferred until a use case appears.

## Out-of-scope (this package)

- TS wrappers / programmatic SDK clients — separate package if needed.
- PowerBI semantic model consumption — that's `pbi-metadata-capture` and PBIP fixtures.
- Cost-modelling automation — out of scope.
